# Bundled test data

`section1/intersection/expr1.h5ad` is the smallest final section-1 input
(common 72-protein panel, two batches).  It is included so a fresh clone can
run the complete CITEpool workflow without downloading external data.

Run it with:

```bash
python experiments/section1/run.py --output results/section1_expr1
```
